The scheduler interface was created with [Create React App](https://github.com/facebookincubator/create-react-app), using ECMAScript 6.


run `npm install` and then `npm run deploy` to deploy to github pages.
